spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client
      #需要从github上读取的资源名称
      profile: dev
      #profile: test
      label: master
      uri: http://localhost:9801
      #SpringCloudConfig获取的服务地址,你自己的 config 服务

